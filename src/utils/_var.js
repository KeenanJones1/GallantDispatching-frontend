const colors = {
 $background: '#3E7E98',
 $black: '#253236',
 $brown: '#9B6A37',
 $footer: '#08154A',
}

const spacing = {
 $letter: '0.25rem',
}

const fonts = {
 $titleFont: 'Times Pro, sans-serif',
 $mainFont: 'IBM Plex Sans',
 $latoFont: 'Lato, sans-serif'
}

const transitions = {
 $primary: 'all 0.3s linear'
}

const shadows = {
 $light: '0 5px 15px rgba(0, 0, 0, 0.1)',
 $dark: '0 5px 15px rgba(0, 0, 0, 0.2)',
}

const radius = {
 $primary: '0.5rem'
}

export {fonts, colors, transitions, shadows, spacing, radius}